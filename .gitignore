node_modules
dist
.DS_Store

# Mọi file .env chứa secret — KHÔNG commit.
.env
.env.local
.env.admin
.env.*.local

# Chỉ các file mẫu/không chứa secret được commit.
!.env.example
!.env.admin.example
!.env.production

supabase/seed-roster.private.sql
supabase/.temp/

# Cấu hình lớp chứa tên học sinh — chỉ giữ lại file mẫu.
admin/*.json
admin/*.csv
!admin/class.example.json

.claude/

_test-session.mjs
